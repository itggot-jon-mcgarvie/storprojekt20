p Your tabs
- if user != nil && user != user.to_s
    ul
        -user.each do |element|
            .row
                li = element["title"]
                a href="/tabs/#{element['tab_id']}" Show tab
- elsif user == user.to_s
    p = user
    
p All tabs
- if result != nil
    ul
        - result.each do |item|
            .row
                li = item["title"]
                a href="/tabs/#{item['tab_id']}" Show tab